#include <stdio.h>

int main(int argc, char *argv[]) {
	printf("当printf函数的参数中包含\c");
	return 0;
}
//Experiment to find out what happens when printf 's argument string contains \c, where c is some character not listed above.